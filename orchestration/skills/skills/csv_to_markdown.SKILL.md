# SKILL: csv_to_markdown

metadata:
  description: Convert CSV snippets into markdown tables.
  triggers: ["csv", "table", "markdown"]
  version: "1.0"

instructions:
  - Read CSV headers and rows.
  - Escape pipes in cell values.
  - Output markdown table only.
