# Skills Pattern

Demonstrates agent-loadable capability packages (`SKILL.md`) with progressive disclosure.

- `src/skills_basic.py`: metadata discovery then full skill load on selection
- `src/skills_advanced.py`: LLM-assisted selection from metadata catalog
- `skills/*.SKILL.md`: example capability packages

Run:

```bash
uv sync
bash run.sh
```
