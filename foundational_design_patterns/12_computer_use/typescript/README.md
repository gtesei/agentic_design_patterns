# Computer Use — TypeScript

**Direct ports of `../src/computer_use_basic.py` and `../src/computer_use_advanced.py`.** Same simulated screenshot-think-act-observe loop, same safety framing, and the same optional Playwright/browser-summary flow.

## Run it

```bash
bash run.sh
```

## Smoke test

```bash
bun test
```
