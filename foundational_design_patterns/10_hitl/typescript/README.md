# Human-in-the-Loop — TypeScript

**Direct ports of `../src/hitl_basic.py`, `../src/hitl_advanced.py`, and `../src/hitl_langgraph.py`.** The TypeScript demos preserve the same approval flows, prompts, revision loops, menus, and audit behavior as the Python versions.

## Run it

```bash
bash run.sh
```

## Smoke test

```bash
bun test
```
