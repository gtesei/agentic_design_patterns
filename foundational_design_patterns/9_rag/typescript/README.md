# RAG — TypeScript

**Direct ports of the Python RAG demos in `../src/`.** Same hybrid retriever baseline, same hosted-file-search fallback flow, same agentic retrieval loop, and the same failure-mode walkthrough.

## Run it

```bash
bash run.sh
```

## Smoke test

```bash
bun test
```
