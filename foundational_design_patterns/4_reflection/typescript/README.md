# Reflection — TypeScript

**Direct ports of `../src/reflection.py` and `../src/reflection_stateful_loop.py`.** Same factorial-code reflection flow in the basic demo and the same iterative blog-post writer with LangGraph state and structured critique in the advanced demo.

## Run it

```bash
bash run.sh
```

## Smoke test

```bash
bun test
```
