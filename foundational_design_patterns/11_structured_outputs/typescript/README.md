# Structured Outputs — TypeScript

**Direct ports of `../src/structured_outputs_basic.py` and `../src/structured_outputs_advanced.py`.** Same invoice-extraction examples, same naive-vs-schema contrast, and the same malformed-input retry/failure-mode walkthrough.

## Run it

```bash
bash run.sh
```

## Smoke test

```bash
bun test
```
