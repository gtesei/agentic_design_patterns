# ReAct — TypeScript

**Direct ports of `../src/react_agent.py` and `../src/react_agent_advanced.py`.** Same mock tool set, same example queries, same explicit Thought → Action → Observation framing in the advanced graph, and the same interactive `run.sh` menu structure.

## Run it

```bash
bash run.sh
```

## Smoke test

```bash
bun test
```
