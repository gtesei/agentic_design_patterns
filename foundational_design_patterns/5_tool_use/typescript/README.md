# TypeScript Companion — Tool Use

This is the TypeScript companion for the Python tool-use chapter.

Canonical chapter:
- `../README.md`

Run:

```bash
bash run.sh
```

Smoke test:

```bash
bun test
```
