{
  "name": "@adp/parallelization",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Parallelization — TypeScript port of src/parallelization.py.",
  "scripts": {
    "demo": "bun run src/parallelization.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@langchain/core": "^1.0.0",
    "@langchain/openai": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.6.0"
  }
}
